#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <map>
#include<string>
using namespace std;


int main() {
    long n,i;
    cin>>n;
    string name[n];
    long phone[n];
    for(i=0;i<n;i++)
    {
        cin>>name[i]>>phone[i];
    }
    string search[100];
    for(int j=0;j<100;j++)
    {
        cin>>search[i];
        
      }
    return 0;
}
